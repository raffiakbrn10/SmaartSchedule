"use client";

import { useRouter, useSearchParams } from "next/navigation";
import { useEffect, useState } from "react";
import { useAuth } from "@/context/AuthContext";
import { api, ApiError } from "@/lib/api";
import type { IntegrationStatus } from "@/types/api";

export default function ProfilePage() {
  const { user, logout } = useAuth(); const router = useRouter(); const params = useSearchParams(); const [status, setStatus] = useState<IntegrationStatus>({ googleLinked: false, telegramLinked: false }); const [message, setMessage] = useState(""); const [busy, setBusy] = useState<"google" | "telegram" | "logout" | null>(null);
  useEffect(() => { api.integrationStatus().then(setStatus).catch((caught) => setMessage(caught instanceof ApiError ? caught.message : "Status integrasi tidak dapat dimuat.")); }, []);
  useEffect(() => { const linked = params.get("google_linked"); if (linked === "success") setMessage("Google Calendar berhasil ditautkan."); else if (linked === "failed") setMessage("Google Calendar gagal ditautkan."); }, [params]);
  async function connectGoogle() { setBusy("google"); setMessage(""); try { const { url } = await api.googleAuthUrl(); window.location.assign(url); } catch (caught) { setMessage(caught instanceof ApiError ? caught.message : "URL Google tidak dapat dibuat."); setBusy(null); } }
  async function connectTelegram() { setBusy("telegram"); setMessage(""); try { const { url } = await api.telegramLink(); window.open(url, "_blank", "noopener,noreferrer"); } catch (caught) { setMessage(caught instanceof ApiError ? caught.message : "Tautan Telegram tidak dapat dibuat."); } finally { setBusy(null); } }
  async function signOut() { setBusy("logout"); await logout(); router.replace("/login"); }
  return <div className="mx-auto max-w-3xl p-5 sm:p-6"><h1 className="mb-6 text-2xl font-bold">Pengaturan profil</h1><div className="space-y-8 rounded-3xl border border-neutral-200/60 bg-white/70 p-6 shadow-xl backdrop-blur-md dark:border-neutral-800 dark:bg-neutral-900/60 sm:p-8"><section className="flex items-center gap-5"><div aria-hidden className="flex h-20 w-20 shrink-0 items-center justify-center rounded-2xl bg-emerald-100 text-3xl font-bold text-emerald-600 dark:bg-cyan-900/50 dark:text-cyan-400">{user?.username.charAt(0).toUpperCase() ?? "U"}</div><div className="min-w-0"><h2 className="truncate text-xl font-bold">{user?.username ?? "Pengguna"}</h2><p className="text-sm text-neutral-500">Akun SmartSchedule</p><span className="mt-2 inline-block rounded-lg bg-emerald-100 px-3 py-1 text-xs font-semibold text-emerald-700 dark:bg-cyan-950/50 dark:text-cyan-400">Sesi terverifikasi</span></div></section>{message && <div role="status" className="rounded-xl border border-amber-200 bg-amber-50 p-3 text-sm text-amber-800 dark:border-amber-900 dark:bg-amber-950/20 dark:text-amber-400">{message}</div>}<section className="border-t border-neutral-200 pt-6 dark:border-neutral-800"><h2 className="text-lg font-bold">Integrasi layanan</h2><p className="mb-4 mt-1 text-sm text-neutral-500">Sinkronkan tugas ke kalender dan aktifkan pengingat deadline.</p><div className="grid gap-4 sm:grid-cols-2"><article className="flex flex-col justify-between gap-5 rounded-2xl border border-neutral-200 bg-neutral-100/50 p-4 dark:border-neutral-800 dark:bg-neutral-800/40"><div><h3 className="font-semibold">📅 Google Calendar</h3><p className="mt-1 text-xs leading-5 text-neutral-500">Jadwal baru otomatis ditambahkan ke kalender pribadimu.</p></div><button disabled={busy !== null} onClick={() => void connectGoogle()} className={`w-full rounded-xl py-2.5 text-xs font-bold transition disabled:opacity-60 ${status.googleLinked ? "bg-neutral-200 text-neutral-700 dark:bg-neutral-800 dark:text-neutral-300" : "bg-emerald-500 text-white hover:bg-emerald-600"}`}>{busy === "google" ? "Membuka Google..." : status.googleLinked ? "Google terhubung (ganti akun)" : "Tautkan Google"}</button></article><article className="flex flex-col justify-between gap-5 rounded-2xl border border-neutral-200 bg-neutral-100/50 p-4 dark:border-neutral-800 dark:bg-neutral-800/40"><div><h3 className="font-semibold">✈️ Telegram Reminder</h3><p className="mt-1 text-xs leading-5 text-neutral-500">Dapatkan pengingat deadline langsung di chat Telegram.</p></div><button disabled={busy !== null} onClick={() => void connectTelegram()} className={`w-full rounded-xl py-2.5 text-xs font-bold transition disabled:opacity-60 ${status.telegramLinked ? "bg-neutral-200 text-neutral-700 dark:bg-neutral-800 dark:text-neutral-300" : "bg-sky-500 text-white hover:bg-sky-600"}`}>{busy === "telegram" ? "Membuat tautan..." : status.telegramLinked ? "Telegram aktif (tautkan ulang)" : "Mulai bot Telegram"}</button></article></div></section><section className="border-t border-neutral-200 pt-6 dark:border-neutral-800"><button disabled={busy !== null} onClick={() => void signOut()} className="rounded-xl border border-red-200 bg-red-50 px-6 py-3 text-sm font-medium text-red-600 transition hover:bg-red-100 focus-visible:outline focus-visible:outline-2 focus-visible:outline-red-500 disabled:opacity-60 dark:border-red-900 dark:bg-red-950/30 dark:text-red-400">{busy === "logout" ? "Keluar..." : "Keluar aplikasi"}</button></section></div></div>;
}
