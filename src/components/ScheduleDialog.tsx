"use client";

import { useEffect, useRef, useState, type FormEvent } from "react";
import { scheduleSchema } from "@/lib/scheduleSchema";
import type { Schedule, ScheduleInput } from "@/types/api";

const blank: ScheduleInput = { judul: "", prioritas: "Medium", status: "Belum Selesai", deadline: "" };
function localDateTime(value: string): string { const date = new Date(value); const shifted = new Date(date.getTime() - date.getTimezoneOffset() * 60_000); return shifted.toISOString().slice(0, 16); }

export function ScheduleDialog({ schedule, open, saving, onClose, onSave }: { schedule: Schedule | null; open: boolean; saving: boolean; onClose(): void; onSave(input: ScheduleInput): Promise<void> }) {
  const [form, setForm] = useState<ScheduleInput>(blank); const [error, setError] = useState(""); const firstInput = useRef<HTMLInputElement>(null);
  useEffect(() => { if (open) { setForm(schedule ? { judul: schedule.judul, prioritas: schedule.prioritas, status: schedule.status, deadline: localDateTime(schedule.deadline) } : blank); setError(""); setTimeout(() => firstInput.current?.focus(), 0); } }, [open, schedule]);
  if (!open) return null;
  async function submit(event: FormEvent) { event.preventDefault(); const result = scheduleSchema.safeParse(form); if (!result.success) { setError(result.error.issues[0]?.message ?? "Data tidak valid."); return; } await onSave({ ...result.data, deadline: new Date(result.data.deadline).toISOString() }); }
  return <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/50 p-4 backdrop-blur-sm" onMouseDown={(event) => { if (event.currentTarget === event.target) onClose(); }}><div role="dialog" aria-modal="true" aria-labelledby="dialog-title" className="w-full max-w-md rounded-3xl border border-neutral-200 bg-white p-6 shadow-2xl dark:border-neutral-800 dark:bg-neutral-900"><div className="flex items-center justify-between"><h2 id="dialog-title" className="text-xl font-bold">{schedule ? "Edit tugas" : "Tambah tugas baru"}</h2><button type="button" onClick={onClose} aria-label="Tutup dialog" className="rounded-lg p-2 hover:bg-neutral-100 dark:hover:bg-neutral-800">✕</button></div>{error && <p role="alert" className="mt-4 rounded-xl bg-red-50 p-3 text-sm text-red-700 dark:bg-red-950/30 dark:text-red-400">{error}</p>}<form onSubmit={(e) => { void submit(e); }} className="mt-5 space-y-4"><div><label htmlFor="title" className="mb-1 block text-sm font-medium">Judul tugas</label><input ref={firstInput} id="title" required maxLength={200} value={form.judul} onChange={(event) => setForm({ ...form, judul: event.target.value })} className="w-full rounded-xl border border-transparent bg-neutral-100 px-4 py-2.5 outline-none focus:ring-2 focus:ring-emerald-500 dark:bg-black dark:focus:ring-cyan-500" /></div><div className="grid grid-cols-2 gap-4"><div><label htmlFor="priority" className="mb-1 block text-sm font-medium">Prioritas</label><select id="priority" value={form.prioritas} onChange={(event) => setForm({ ...form, prioritas: event.target.value as ScheduleInput["prioritas"] })} className="w-full rounded-xl bg-neutral-100 px-3 py-2.5 outline-none focus:ring-2 focus:ring-emerald-500 dark:bg-black">{["Rendah", "Medium", "Tinggi"].map((value) => <option key={value}>{value}</option>)}</select></div><div><label htmlFor="status" className="mb-1 block text-sm font-medium">Status</label><select id="status" value={form.status} onChange={(event) => setForm({ ...form, status: event.target.value as ScheduleInput["status"] })} className="w-full rounded-xl bg-neutral-100 px-3 py-2.5 outline-none focus:ring-2 focus:ring-emerald-500 dark:bg-black">{["Belum Selesai", "Sedang Berjalan", "Selesai"].map((value) => <option key={value}>{value}</option>)}</select></div></div><div><label htmlFor="deadline" className="mb-1 block text-sm font-medium">Deadline</label><input id="deadline" type="datetime-local" required value={form.deadline} onChange={(event) => setForm({ ...form, deadline: event.target.value })} className="w-full rounded-xl bg-neutral-100 px-4 py-2.5 outline-none focus:ring-2 focus:ring-emerald-500 dark:bg-black" /></div><div className="flex gap-3 border-t border-neutral-100 pt-4 dark:border-neutral-800"><button type="button" onClick={onClose} className="flex-1 rounded-xl bg-neutral-100 py-2.5 font-medium hover:bg-neutral-200 dark:bg-neutral-800 dark:hover:bg-neutral-700">Batal</button><button disabled={saving} className="flex-1 rounded-xl bg-emerald-500 py-2.5 font-medium text-white hover:bg-emerald-600 disabled:opacity-60 dark:bg-cyan-600">{saving ? "Menyimpan..." : "Simpan"}</button></div></form></div></div>;
}
